This is a README for the FearOfJava repository.  
This time I created the repository via command line
instead of using the web gui functionality at
github.com.

/** 
    when I tried to push this it failed, probably
	because I did not create a repository first on
	github.  I thought I could do it all via the 
	command line here, but either that's not the 
	case or (more likely) I have to learn how.
*/


